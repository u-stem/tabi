{
  "buildCommand": "sh -c 'cd ../.. && bun run db:migrate && DATABASE_URL=$MIGRATION_URL bun run db:ensure-faqs-table && DATABASE_URL=$MIGRATION_URL bun run db:seed-faqs' && next build --webpack",
  "framework": "nextjs",
  "fluid": true
}
