services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: tabi
      POSTGRES_PASSWORD: tabi
      POSTGRES_DB: tabi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
