{
  "name": "tabi",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "format": "turbo format",
    "check": "turbo check",
    "check-types": "turbo check-types",
    "db:generate": "bun run --filter @tabi/api db:generate",
    "db:migrate": "bun run --filter @tabi/api db:migrate",
    "db:push": "bun run --filter @tabi/api db:push",
    "db:studio": "bun run --filter @tabi/api db:studio",
    "db:seed": "bun run --filter @tabi/api db:seed",
    "setup": "bun run scripts/setup.ts"
  },
  "packageManager": "bun@1.3.2",
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "turbo": "^2"
  }
}
