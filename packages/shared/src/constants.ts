import type { ScheduleCategory } from "./schemas/schedule";

export const DEFAULT_SCHEDULE_CATEGORY: ScheduleCategory = "sightseeing";

// Dummy email domain assigned to registered users who haven't set a real email
export const DUMMY_EMAIL_DOMAIN = "sugara.local";
